# План разработки Stroop-теста

## Обзор проекта
Stroop-тест — интерактивное веб-приложение для оценки когнитивного контроля. Пользователь видит слово, обозначающее цвет, написанное цветным шрифтом, и должен выбрать цвет шрифта, игнорируя семантическое значение слова.

**Технологический стек:**
- Frontend: React 19 с TypeScript
- Сборка: Vite
- Стилизация: CSS Modules
- Тестирование: Vitest + React Testing Library
- Хранение состояния: React Context + useReducer

**Текущее состояние:** проект инициализирован с базовым шаблоном Vite + React, есть файлы `src/App.tsx`, `src/main.tsx`, `package.json` с зависимостями React 19. Структура папок создана согласно упрощённой спецификации: присутствуют папки `components/` (с подпапками `Stimulus/`, `ButtonPanel/`, `Timer/`, `Results/`, `DifficultySelector/`), `context/` (реализован `StroopContext`), `utils/`, `types/`. Папка `hooks/` и компонент `Stats/` исключены из структуры.

## Цели разработки
1. Реализовать все основные экраны (приветствие, тестирование, результаты)
2. Внедрить генерацию стимулов с конгруэнтностью
3. Реализовать три уровня сложности с разными настройками
4. Добавить расчёт метрик и сохранение истории
5. Обеспечить адаптивный и визуально приятный UI
6. Написать тесты для ключевых функций

## Пошаговый план

### Этап 1: Настройка проекта и базовая структура (1 день)
**Задачи:**
- [ ] Создать структуру папок согласно упрощённой спецификации:
  - `src/components/` с подпапками: `Stimulus/`, `ButtonPanel/`, `Timer/`, `Results/`, `DifficultySelector/`
  - `src/context/`
  - `src/utils/`
  - `src/types/`
- [ ] Настроить абсолютные импорты через `tsconfig.json`
- [ ] Установить дополнительные зависимости (если нужны):
  - `recharts` для графиков (опционально)
  - `date-fns` для форматирования дат
- [ ] Настроить ESLint и Prettier для единообразия кода
- [ ] Создать базовые TypeScript-типы в `src/types/index.ts`

**Результат:** готовая структура проекта, настроенные инструменты.

### Этап 2: Разработка состояния приложения (Context + Reducer) (1 день)
**Задачи:**
- [ ] Определить интерфейсы состояния (`StroopState`, `Stimulus`, `Answer`, `Metrics`) в `src/types/index.ts`
- [ ] Создать контекст `StroopContext` в `src/context/StroopContext.tsx`
- [ ] Реализовать редьюсер с экшенами: `initTest`, `recordAnswer`, `nextStimulus`, `completeTest`, `resetTest`, `pauseTest`, `resumeTest`
- [ ] Написать провайдер контекста, который оборачивает приложение
- [ ] Создать хук `useStroop` для удобного доступа к контексту

**Результат:** централизованное состояние приложения, готовое к использованию компонентами.

### Этап 3: Утилиты и генерация стимулов (1 день)
**Задачи:**
- [ ] Создать файл `src/utils/constants.ts` с константами цветов, слов, уровней сложности
- [ ] Реализовать `src/utils/stimulus.ts` с функцией `generateStimuli(difficulty, count)`
  - Учитывать подмножества цветов для каждого уровня
  - Регулировать соотношение конгруэнтных/неконгруэнтных
  - Для сложного уровня — только неконгруэнтные
- [ ] Реализовать `src/utils/metrics.ts` с функциями расчёта метрик:
  - `calculateMetrics(answers, stimuli)`
  - Среднее время реакции, точность, индекс интерференции, скорость ответа
- [ ] Создать `src/utils/helpers.ts` с вспомогательными функциями (форматирование времени, генерация ID)

**Результат:** готовые утилиты для генерации данных и расчётов.

### Этап 4: Реализация логики в утилитах и контексте (1 день)
**Задачи:**
- [ ] Интегрировать генерацию стимулов из `src/utils/stimulus.ts` в контекст
- [ ] Реализовать таймер сессии внутри компонента `Timer` с использованием `setInterval`
- [ ] Добавить вычисление метрик на лету с помощью функций из `src/utils/metrics.ts`
- [ ] Реализовать сохранение/загрузку истории через `localStorage` прямо в контексте

**Результат:** вся бизнес-логика работает через контекст и утилиты, без отдельных хуков.

### Этап 5: Компоненты UI (2 дня)
**Задачи:**
- [ ] **Компонент `DifficultySelector`** (`src/components/DifficultySelector/`):
  - Отображает три уровня сложности с описанием
  - Кнопка "Начать" вызывает `initTest`
- [ ] **Компонент `Stimulus`** (`src/components/Stimulus/`):
  - Принимает `word` и `color`, отображает слово с цветным шрифтом
  - Анимация fade-in
- [ ] **Компонент `ButtonPanel`** (`src/components/ButtonPanel/`):
  - Рендерит кнопки цветов в зависимости от уровня сложности
  - При нажатии диспатчит `recordAnswer` и `nextStimulus`
  - Визуальная обратная связь (scale-down)
- [ ] **Компонент `Timer`** (`src/components/Timer/`):
  - Показывает прошедшее время сессии
  - Использует внутренний таймер на `setInterval`
- [ ] **Компонент `Results`** (`src/components/Results/`):
  - Таблица с итоговыми метриками
  - Кнопки "Повторить" (сброс и начало новой сессии) и "История"

**Результат:** все основные UI-компоненты готовы к интеграции.

### Этап 6: Интеграция компонентов и маршрутизация (1 день)
**Задачи:**
- [ ] В `App.tsx` реализовать условный рендеринг экранов на основе состояния:
  - Если статус `idle` — показывать `DifficultySelector`
  - Если `running` или `paused` — показывать `Stimulus`, `ButtonPanel`, `Timer`
  - Если `completed` — показывать `Results`
- [ ] Настроить плавные переходы между экранами
- [ ] Подключить все компоненты к контексту
- [ ] Реализовать кнопки управления (Пауза, Завершить досрочно)

**Результат:** рабочее приложение с полным циклом тестирования.

### Этап 7: Стилизация и адаптивность (1 день)
**Задачи:**
- [ ] Создать CSS-модули для каждого компонента
- [ ] Реализовать адаптивный дизайн согласно спецификации:
  - Десктоп: горизонтальное расположение кнопок
  - Планшет: кнопки в одну строку с меньшими отступами
  - Мобильный: кнопки в 2 колонки, уменьшенный шрифт стимула
- [ ] Применить цветовую палитру из спецификации
- [ ] Добавить анимации: fade-in стимула, scale-down кнопок, плавный таймер
- [ ] Протестировать на разных размерах экрана

**Результат:** визуально приятное, адаптивное приложение.

### Этап 8: Сохранение данных и история (1 день)
**Задачи:**
- [ ] Реализовать сохранение каждой сессии в `localStorage`
  - Структура: массив объектов сессии с датой, уровнем сложности, метриками
- [ ] Добавить загрузку истории при монтировании приложения
- [ ] Создать экран истории (таблица с предыдущими попытками, фильтрация по уровню)
- [ ] Реализовать экспорт истории в CSV (опционально)

**Результат:** сохранение прогресса пользователя между сессиями.

### Этап 9: Тестирование (1 день)
**Задачи:**
- [ ] Настроить Vitest и React Testing Library
- [ ] Написать модульные тесты для утилит (`stimulus.ts`, `metrics.ts`)
- [ ] Написать интеграционные тесты для компонентов (`ButtonPanel`, `Stimulus`, `Timer`)
- [ ] Написать тесты для контекста (`StroopContext`)
- [ ] Написать E2E-тест с помощью Playwright (опционально)

**Результат:** покрытие ключевой логики тестами.

### Этап 10: Дополнительный функционал и полировка (1 день)
**Задачи:**
- [ ] Реализовать визуализацию распределения времени реакции (гистограмма с Recharts)
- [ ] Добавить режим "на время" (60 секунд)
- [ ] Улучшить UX: звуковые сигналы, подсказки, прогресс-бар
- [ ] Оптимизировать производительность (useMemo, useCallback)
- [ ] Провести рефакторинг и очистку кода
- [ ] Написать документацию для разработчиков (комментарии в коде)

**Результат:** полнофункциональное приложение с дополнительными возможностями.

### Этап 11: Деплой и финальные проверки (0.5 дня)
**Задачи:**
- [ ] Собрать production-сборку (`npm run build`)
- [ ] Протестировать сборку локально (`npm run preview`)
- [ ] Развернуть на Vercel / Netlify
- [ ] Проверить работу на разных устройствах и браузерах
- [ ] Исправить найденные баги

**Результат:** приложение доступно онлайн.

## Оценка времени
Общая оценка: **10,5 дней** (при работе в одиночку, 8 часов в день). Можно сократить за счёт параллельной работы над некоторыми этапами.

## Приоритеты
1. **Высокий приоритет:** Этапы 1–6 (базовый функционал)
2. **Средний приоритет:** Этапы 7–9 (стили, сохранение, тесты)
3. **Низкий приоритет:** Этап 10 (дополнительный функционал)

## Риски и смягчение
- **Сложность генерации стимулов:** тщательно протестировать алгоритм на всех уровнях сложности.
- **Производительность анимаций:** использовать CSS-анимации вместо JavaScript.
- **Совместимость браузеров:** проверить на основных браузерах (Chrome, Firefox, Safari).
- **Объём состояния:** использовать useReducer для эффективного управления.

## Критерии успеха
- Приложение работает без ошибок в соответствии со спецификацией.
- Пользователь может пройти тест на любом уровне сложности.
- Метрики рассчитываются корректно и сохраняются.
- UI адаптивен и интуитивно понятен.
- Код покрыт тестами и хорошо документирован.

---
*Документ создан на основе SPECIFICATION.md. Версия 1.0.*